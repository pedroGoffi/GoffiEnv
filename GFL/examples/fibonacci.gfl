#import "std/std.gfl"

proc main(): void {
  var lim: i64 = stoi(input("Input the max fib num: "));
  var x0:  i64  = 0;
  var x1:  i64  = 1;
  var acc: i64  = 0;
  while x0 < lim {
    x0  = x1 + acc;
    x1  = acc;
    acc = x0;
    __print__(x0);
  }  
}

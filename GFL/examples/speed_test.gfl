MAX_DEPTH :: 10
N_FACT    :: 5

// #recursive
proc fact(x: i64):*i64 {

  var v: i64 = 1 if x < 2 else fact(x-1);
  return x*v;
}
  

proc main(): void {
  var i: i64 = 0;
  while i < MAX_DEPTH {
    __print__(
      fact(i)
    );
    i = i + 1;
  }
}


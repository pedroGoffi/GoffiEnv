#import "std/std.gfl"


proc main(x: i64, y: **char): void {
  __print__(x, y);
  //puts(y);
  exit(1);
  var i: i64 = 10;
  while i < 100 + 1 {
    __print__(
      sum_range(i)
    );
   
    inc64(&i);
  }
}

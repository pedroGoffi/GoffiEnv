proc main(){
  var x: i64;
  x = 10;  
  // push 10
  // COMPILE_EXPR
  // pop  rbx
  // mov word [rsp - offset], rbx
  var ptr: *i64;
  ptr = x;

  // mov  rax, word [rsp - offset]
  // push rax
  // COMPILE_EXPR
  // pop rbx
  // lea rax, [rsp - ptr_offset]
  // NOTE: if *x = ...
  //     Then mov rax, [rax]
  // mov rax, rbx
  
  
}
